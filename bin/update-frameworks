#!/usr/bin/python3

from clickreviews import frameworks

def main():
    frameworks.get_frameworks_file()

if __name__ == '__main__':
    try:
        main()
    except KeyboardInterrupt:
        print('Aborted.', file=sys.stderr)
        sys.exit(1)
